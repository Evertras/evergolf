// Coords represents a point on the hole from the top left corner of the map.
interface Coords {
  xYards: number;
  yYards: number;
}

interface CoordsPixels {
  xPixels: number;
  yPixels: number;
}
